# Minimal Python dependencies for local infrastructure verification and tooling
# Note: Spark runs containerized inside Docker cluster. Do NOT install pyspark globally on Windows host.
kafka-python==2.0.2
psycopg2-binary>=2.9.9
requests>=2.31.0
python-dotenv>=1.0.0
